<div>
    <h1>Test Homepage</h1>
</div>
<div class="text-center">
    <h2>{{product_image}}</h2>
    <h2>{{product_name}}</h2>
    <p>{{product_descritption}}</p>
    <h2>{{product_price}}</h2>
    <p>Posted by {{user.name}} on {{format_date date_created}}</p>
    <p>
        <span img="img" href="shoe"></span>
    </p>
    <p>
        <button class="btn btn-primary">Buy</button>
    </p>
</div>

